import '@testing-library/jest-dom/vitest';

/**
 * Global test setup. Add jest-dom matchers so `expect(el).toBeInTheDocument()`
 * etc. work in Vitest. Node-env tests ignore this (happy-dom-specific matchers
 * simply aren't called there).
 */
