/**
 * Public UI primitive exports. Import from `@/components/ui` so the
 * call-site doesn't need to know which file each primitive lives in.
 */
export { FadeIn, ScaleIn, Stagger } from './Motion';
export { CountUp } from './CountUp';
export { Skeleton } from './Skeleton';
export { StatCard } from './StatCard';
export { PageContainer } from './PageContainer';
